<div class="container">
  <div class="row">
    <div class="col-md-offset-3 col-md-6">
      <p id="notice"><%= notice %></p>

      <h1>Elenme Sebepleri</h1>

      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th colspan="2"></th>
          </tr>
        </thead>

        <tbody>
          <% @causes.each do |cause| %>
            <tr>
              <td><%= cause.name %></td>
              <td><%= link_to 'Düzenle', edit_cause_path(cause) %></td>
              <td><%= link_to 'Sil', cause, method: :delete, data: { confirm: 'Emin misin?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>

      <%= link_to 'New Cause', new_cause_path %>
    </div>    
  </div>

</div><!-- /.container -->