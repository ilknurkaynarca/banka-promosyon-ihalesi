<div class="container">
  <div class="row">
    <div class="col-md-offset-3 col-md-6">
      <p id="notice"><%= notice %></p>

      <h1>Bankalar</h1>

      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @banks.each do |bank| %>
            <tr>
              <td><%= bank.name %></td>
              <td><%= link_to 'Düzenle', edit_bank_path(bank) %></td>
              <td><%= link_to 'Sil', bank, method: :delete, data: { confirm: 'Emin misin?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>

      <%= link_to 'New Bank', new_bank_path %>
    </div>    
  </div>

</div><!-- /.container -->


